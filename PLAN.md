# PLAN

1. read input xml file(s) from a given directory 
2. convert xml files of godot documentation to 
2.1 -> implies I have to map the generate xml to a markdown 
2.2 -> optionally expand the tool to convert to other 
3. output to a given directory from cmdline
